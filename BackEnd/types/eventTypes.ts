export type eventData = {
    eventHostId: string,
    eventId: string,
    eventHost: string,
    title: string,
    description: string,
    eventTime: Date,
    eventDate: Date
}

export type userData = {
    id: string,
    name: string
}