export type eventData = {
    eventHostId: string,
    eventHost: string,
    title: string,
    description: string,
    eventDate: string,
    eventTime: string
}

export type userData = {
    id: string,
    name: string
}